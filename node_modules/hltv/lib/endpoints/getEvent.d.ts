import FullEvent from '../models/FullEvent';
import HLTVConfig from '../models/HLTVConfig';
declare const getEvent: (config: HLTVConfig) => ({ id }: {
    id: number;
}) => Promise<FullEvent>;
export default getEvent;
